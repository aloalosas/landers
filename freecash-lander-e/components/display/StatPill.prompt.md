Glassy stat pill for the social-proof strip.

```jsx
<div style={{display:'flex', gap:10}}>
  <StatPill icon="users" value="500K+" label="Active Users" />
  <StatPill icon="cash" value="$30M+" label="Paid Out" />
</div>
```

Icons: `users` · `cash` · `bolt` · `check`. Keep values honest — use placeholders until you have real numbers.
